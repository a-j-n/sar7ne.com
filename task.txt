let's build our auth system it will be only social login with twitter and facebook ,
our ui with be with shadecn and modern ui
our website will competitor sarhne.com
our domain will be sar7ne.com
we will have fixed navbar in the buttom with 3 tabs  explore,inbox , profile
explore tap will be public , inbox will need login , profile will need login
every user will need a unique usernam he can set it and every users have public avatar
also every user will have subdomain example username.sar7ne.com  this sub domain will desplay the public profile of the user
and section for visitor to send an anonymous massage

📌 Project Requirement: sar7ne.com (Competitor to sarhne.com)

1. Authentication System
	•	Authentication will be social login only.
	•	Supported providers:
	•	Twitter
	•	Facebook
	•	No traditional email/password login.

⸻

2. UI & Design
	•	UI framework: shadcn/ui with a modern and minimal style.
	•	Website will be fully responsive (mobile-first design).
	•	Fixed bottom navigation bar with 3 main tabs:
	1.	Explore → Public content, accessible without login.
	2.	Inbox → Requires user login.
	3.	Profile → Requires user login.

⸻

3. User System
	•	Every user must have a unique username.
	•	Each user has a public avatar (customizable).
	•	User profile will be accessible via subdomain:
	•	Example: username.sar7ne.com
	•	Public profile page contains:
	•	Username
	•	Avatar
	•	Bio (optional, short text about the user)
	•	Section for visitors to send anonymous messages.

⸻

4. Features

Explore Tab (Public)
	•	Displays random or trending users.
	•	Option to search by username.

Inbox Tab (Private)
	•	Displays all anonymous messages received by the logged-in user.
	•	Messages can be marked as read/unread or deleted.

Profile Tab (Private)
	•	Displays the user’s own profile (same as their public subdomain).
	•	User can:
	•	Edit username (must remain unique).
	•	Change avatar.
	•	Update bio.

Public User Profile (Subdomain)
	•	Visitors can see: avatar, username, bio.
	•	Visitors can send an anonymous message to the user.
	•	No login required for visitors.

⸻

5. Technical Requirements
	•	Domain: sar7ne.com
	•	Subdomain routing for user profiles (e.g., username.sar7ne.com).
	•	Database tables:
	•	users → id, username, avatar, provider_id, provider_type, created_at
	•	messages → id, sender_ip (optional), receiver_id, message_text, created_at, status
	•	Security:
	•	Limit message spam (captcha or rate limit per IP).
	•	Validate unique usernames on signup.

⸻

6. Future Enhancements (Optional Roadmap)
	•	Add Google login as extra social option.
	•	Allow users to reply privately to anonymous messages.
	•	Add dark mode toggle.
	•	Support profile customization (cover photo, theme color).
	•	Explore tab enhancements: trending hashtags, top profiles.
